# 05. AI 利用ログ

## 1. 概要

このドキュメントは、本課題の遂行において AI（Codex/Claude）をどのように開発プロセスに組み込んだかを記録するものです。

---

## 2. 利用履歴

### 手動で生成したプロジェクトに各種ドキュメントを追加
* **AIにさせたこと:**
プロジェクト作成前にGeminiで生成したドキュメント類をdocsディレクトリに追加し、それぞれのドキュメントが指定された要件に基づいているか＆各ドキュメントに一貫性があるかを確認。
* **利用の流れ:**
（Gemini）指定された要件に基づき01_requirements.md~05_ai_usage_log.mdを作成
（手動）npm create react-router@latest ts-blog-app -- --typescriptで新しくプロジェクトを作成
（手動） docsディレクトリを作成し、事前に指定された要件に基づいてGeminiで作成したドキュメントを追加
（Codex）プロジェクト全体を確認し、それぞれのドキュメントが指定された要件に基づいているか＆各ドキュメントに一貫性があるかを確認。要件に対しドキュメント側に過不足がある場合は理由を踏まえて全てリストアップする。
* **人間のチェック観点:**
リストアップされた過不足部分が今後の実装によって補完されるものなのか、すでに実装の方向性が間違っていることによるズレなのかを確認する。
ドキュメントを参照した上で開発のロードマップを出力してもらい、確認し問題なさそうであれば実装開始。

### インメモリ用データモジュールの理解
* **AIにさせたこと:**
  - 「インメモリ用データモジュールとは何か」について説明を求めた
  - 実装計画で言及されていた `app/data/posts.server.ts` の役割と特徴を理解したい

* **利用の流れ:**
  1. 実装計画書を読んで、インメモリ用データモジュールについてわからない点があった
  2. AIに「インメモリ用データモジュールとは」と質問した
  3. AIから主な特徴、実装内容、使用目的について説明を受けた

* **人間のチェック観点:**
  - 説明内容が実装計画書と整合しているか確認
  - サーバー専用モジュール（`.server.ts`）の概念が理解できたか
  - インメモリデータの特性（再起動で消える）を把握できたか

* **学んだこと・メモ:**
  - **サーバー専用モジュール**: `.server.ts` 拡張子で、クライアントからは参照しない。サーバー側でのみ実行される。
  - **データストレージ**: データベースやファイルは使わない。サーバーのメモリ内（グローバル変数など）でデータを保持。サーバー再起動でデータは消える。
  - **実装内容**: `Post` 型を定義し、モジュールスコープに `let posts: Post[] = []` を保持。`getPosts()`, `getPost()`, `createPost()`, `deletePost()` などの `async` 関数をエクスポート。
  - **使用目的**: React Router の `loader` / `action` から呼び出し、記事データの CRUD 操作を提供。

  要約すると、データベースを使わずに、サーバーのメモリ上でブログの記事データを管理するモジュールです。開発・学習用途に適した簡易的な実装です。

### posts.server.tsの実装内容の理解確認
* **AIにさせたこと:**
  - `app/data/posts.server.ts`の各処理の流れについて自分の理解が正しいか判定してもらった
  - `Promise<Post[]>`の意味について説明を求めた
  - `slice()`と`clonePost`の役割の違いについて説明を求めた

* **利用の流れ:**
  1. `posts.server.ts`を実装した後、自分が理解した処理の流れを箇条書きでまとめた
  2. AIに理解が正しいか判定してもらい、不正確な部分を指摘してもらった
  3. `Promise<Post[]>`という型アノテーションの意味がわからなかったので説明を求めた
  4. `slice()`で配列をコピーしているのに`clonePost`でオブジェクトもクローンしている理由が理解できなかったため、役割の違いについて説明を求めた

* **人間のチェック観点:**
  - 指摘された不正確な理解を修正し、正確な処理フローを把握できたか
  - 非同期処理とPromiseの関係性を理解できたか
  - 浅いコピー（`slice()`）と深いコピー（`clonePost`）の違いを理解できたか

* **学んだこと・メモ:**
  - **Promise<Post[]>**: `async function`は自動的に返り値を`Promise`でラップする。`Promise<Post[]>`は「将来、`Post[]`型の配列を返す約束」を表す型。
  - **getPosts()の処理フロー**: `slice()`で配列をコピー → `sort()`でソート → `map(clonePost)`で各要素をクローン
  - **slice()の役割**: 配列レベルでの浅いコピー。元の配列の順序や長さへの変更を防ぐ。ただし配列内のオブジェクト自体は同じ参照を共有する。
  - **clonePost()の役割**: オブジェクトレベルでの深いコピー。各Postオブジェクト（特に`createdAt`のDate）を新しいオブジェクトとして作成し、返り値のオブジェクトを変更しても元の`posts`配列に影響しないようにする。
  - **両方必要な理由**: `slice()`だけでは配列内のオブジェクトが共有されてしまうため、`clonePost()`で各要素もクローンする必要がある。これにより、呼び出し側が結果を変更しても元のデータに影響しない。

### SSR/CSRの判定
* **AIにさせたこと:**
  - プロジェクト全体を確認し、画面描画がSSRかCSRかを根拠とともに判定してもらった

* **利用の流れ:**
  1. `react-router.config.ts`や`package.json`などをもとにレンダリング方式を知りたいという疑問を提示
  2. AIが設定値やサーバー用依存関係、`.server.ts`ファイルの存在などを参照してSSRであると結論付けた

* **人間のチェック観点:**
  - 指摘された設定値（`ssr: true`）が実際にファイルへ反映されているか
  - 依存関係やビルド成果物がSSR構成と整合しているか

* **学んだこと・メモ:**
  - React Router v7では`react-router.config.ts`がSSRを切り替える中心設定になる
  - `.server.ts`命名や`@react-router/node`依存がSSRの根拠として重要

### SSRレンダリングフローの整理
* **AIにさせたこと:**
  - `npm run dev`/`npm run start`で起動後、リクエスト→レスポンスまでにどのファイル・関数が実行されるかを詳細に解説してもらった

* **利用の流れ:**
  1. SSR時の処理経路が把握できなかったため、起動コマンドからブラウザ表示までの全体像を質問
  2. AIが`build/server/index.js`の`handleRequest`や`ServerRouter`の動きを中心に、loader/actionとデータモジュールの呼び出し順を整理

* **人間のチェック観点:**
  - 説明された関数やファイルが実際に存在し、コードと一致しているか
  - loader/actionの呼び出し順が想定どおりか

* **学んだこと・メモ:**
  - `renderToPipeableStream`でHTMLをストリーミングする仕組みと、`@react-router/serve`がビルド済みサーバーコードを実行する流れ
  - ルート解決後に親→子順でloaderが実行される点
  - `Layout`→`App`→`Outlet`→各ルートコンポーネントのデータ受け渡しイメージ

### SSR関連用語の深掘り
* **AIにさせたこと:**
  - Nodeハンドラや`renderToPipeableStream`の意味、HTMLストリーミング、loader/action実行順など用語レベルの疑問を解消してもらった

* **利用の流れ:**
  1. 前段の説明で出てきたキーワードの具体像が曖昧だったため個別に質問
  2. AIが各用語の役割・処理内容・関連ファイルを紐付けて解説

* **人間のチェック観点:**
  - 回答がReact Routerの公式仕様と矛盾していないか
  - 自分の理解が進んだかどうかを、ファイルを参照しながら確認

* **学んだこと・メモ:**
  - Nodeハンドラ=Node.jsでHTTPを処理する入口関数であり、本プロジェクトでは`handleRequest`
  - `useLoaderData`がサーバーで取得した値をクライアントへ橋渡しする仕組み
  - ハイドレーションはSSR済みHTMLに対してReactがイベント等を再アタッチする工程

### JSX変換に関する疑問解消
* **AIにさせたこと:**
  - TypeScriptで書いたのにビルド済みコードで`jsx`関数が見える理由を説明してもらった

* **利用の流れ:**
  1. `build/server/index.js`に`import { jsx, jsxs }`が現れる理由を質問
  2. AIが自動JSXトランスフォームの仕組みとして説明し、`.tsx`→`jsx()`呼び出しへの変換だと回答

* **人間のチェック観点:**
  - 新しいJSXランタイム（automatic runtime）の知識と一致するか
  - 実際の`.tsx`ソースとビルド結果が対応しているか

* **学んだこと・メモ:**
  - `.tsx`で書いたJSXはビルド後に`jsx/jsxs`ヘルパー呼び出しへ変換され、拡張子が`.jsx`になるわけではない

### 図解プロンプト作成の支援
* **AIにさせたこと:**
  - これまでの会話内容を網羅的に図解してもらうための最適なプロンプト文を生成してもらった

* **利用の流れ:**
  1. 別AIへ図解を依頼するためのプロンプトを作成したかった
  2. AIに要件（図の種類、説明対象、強調したいポイント）をまとめた依頼文を作ってもらった

* **人間のチェック観点:**
  - プロンプトに漏れなく必要な観点が含まれているか
  - 実際に図解AIへ渡した際に期待通りの結果が得られるか

* **学んだこと・メモ:**
  - 図解を依頼する際は対象範囲とアーティファクト種別（flowchart/sequenceDiagramなど）を明記すると精度が上がる
  - 具体的な関数名やファイル名を列挙しておくと誤解が減る

### loader/actionのテスト仕様書作成
* **AIにさせたこと:**
  - `loader` および `action` 関数のテストコードを実装するための仕様書を作成してもらった
  - 各ルートファイル（`_index.tsx`, `posts.$postId.tsx`, `posts.new.tsx`）で定義されている `loader` / `action` のテストケースを網羅的に定義してもらった

* **利用の流れ:**
  1. テストコードを実装する前に、仕様書を作成してテスト方針を明確にしたいと考えた
  2. プロジェクト構造を確認し、既存の `loader` / `action` の実装内容を把握した
  3. AIにテスト仕様書の作成を依頼し、正常系・異常系のテストケース、テスト環境・ツール選定、実装ガイドラインを含む包括的な仕様書を生成してもらった
  4. 生成された仕様書を `docs/08_test_specification.md` として保存した

* **人間のチェック観点:**
  - 仕様書に記載されたテストケースが、実際の `loader` / `action` の実装と整合しているか確認
  - テスト対象の関数（`loader` 3つ、`action` 2つ）がすべてカバーされているか確認
  - 異常系のテストケース（エラーレスポンス、バリデーションエラーなど）が適切に網羅されているか確認
  - テストツールの選定（Vitest）がプロジェクト構成と整合しているか確認

* **学んだこと・メモ:**
  - **テストフレームワーク選定**: ViteプロジェクトではVitestが統合しやすく、TypeScriptのサポートも良好
  - **テスト対象の分離**: `loader` / `action` 関数はコンポーネントから分離して単体テストとして実装する
  - **テストケースの網羅性**: 正常系だけでなく、異常系（エラーケース、バリデーションエラーなど）も適切にテストする必要がある
  - **テストデータ管理**: テスト間での独立性を保つため、`beforeEach` / `afterEach` でデータを初期化・クリーンアップする
  - **エラーレスポンスの検証**: `throw new Response(...)` された場合の検証方法を理解した
  - **リダイレクトの検証**: `redirect()` のレスポンスステータスコードや `Location` ヘッダーの確認方法を理解した
  - 仕様書を作成することで、テスト実装前にテスト方針を明確にでき、実装時の迷いが減る

### loader/actionのテストコード実装
* **AIにさせたこと:**
  - テスト仕様書（`08_test_specification.md`）に基づいて、実際のテストコードを実装してもらった
  - Vitestの設定ファイル作成、テスト用ヘルパー関数の追加、各ルートファイルに対応するテストファイルの作成を依頼した

* **利用の流れ:**
  1. テスト仕様書を作成した後、実際にテストコードを実装したいと考えた
  2. AIにテストコードの実装を依頼し、以下の作業を実施してもらった:
     - Vitestとテスト関連の依存関係のインストール
     - `vitest.config.ts`の作成
     - `package.json`にテストスクリプトの追加
     - `app/data/posts.server.ts`にテスト用の`clearAllPosts()`関数の追加
     - `app/routes/_index.test.tsx`の作成（2テストケース）
     - `app/routes/posts.$postId.test.tsx`の作成（6テストケース）
     - `app/routes/posts.new.test.tsx`の作成（6テストケース）
  3. 実装後、型エラーが発生したため、`Route.LoaderArgs`と`Route.ActionArgs`の型定義に必要な`unstable_pattern`プロパティの追加と型アサーション（`as unknown as`）の使用を修正してもらった
  4. 全14テストケースが正常にパスすることを確認

* **人間のチェック観点:**
  - 実装されたテストコードが仕様書の要件を満たしているか確認
  - テストケースが正常系・異常系を適切にカバーしているか確認
  - 型エラーが解決され、TypeScriptの型チェックが通るか確認
  - テスト実行が成功し、すべてのテストケースがパスするか確認

* **学んだこと・メモ:**
  - **Vitest設定**: React Routerのプラグインはテスト環境では不要なため、`vitest.config.ts`では`tsconfigPaths`のみを使用
  - **テスト用ヘルパー関数**: データストアをクリアする`clearAllPosts()`関数を追加し、`beforeEach`で呼び出すことでテスト間の独立性を保証
  - **型アサーションの必要性**: `Route.LoaderArgs`や`Route.ActionArgs`には`unstable_pattern`プロパティが必要だが、テストでは使用しないため`as unknown as`で型アサーションを使用
  - **Requestオブジェクトの作成**: loader/actionの引数としてWeb標準の`Request`オブジェクトを作成し、必要に応じて`FormData`を`body`として設定
  - **テスト実行**: `npm test -- --run`でテストを実行し、`npm run typecheck`で型チェックも併せて確認

### loader/actionテストコードの書き方の特徴についての確認
* **AIにさせたこと:**
  - 実装したテストコードを確認し、loader/actionをテストする際に通常のテストコードとは異なる書き方になった箇所を説明してもらった

* **利用の流れ:**
  1. テストコードを実装した後、loader/action特有のテストコードの書き方について疑問を持った
  2. AIに実装したテストコードを確認してもらい、通常のテストコードと異なる箇所を説明してもらった

* **人間のチェック観点:**
  - 説明された違いが実際のテストコードと一致しているか確認
  - loader/action特有の書き方が理解できたか確認
  - 今後のテストコード作成時に同様のパターンを適用できるか確認

* **学んだこと・メモ:**
  - **Route.LoaderArgs/Route.ActionArgs型を使ったモックオブジェクト**: React Routerが生成する型定義に合わせたモックオブジェクトを作成する必要がある。通常のテストでは関数の引数を直接渡すが、loader/actionでは特定の構造のオブジェクトを渡す必要がある
  - **unstable_patternプロパティと型アサーション**: React Router v7の型定義では`unstable_pattern`が必須だが、テストでは使用しないため、空文字列を設定し`as unknown as`で型アサーションを使用する必要がある
  - **Requestオブジェクトの直接作成**: loader/actionではWeb標準の`Request`オブジェクトを作成し、特にactionでは`FormData`を`body`として設定する必要がある。通常のテストでは関数の引数としてオブジェクトを直接渡すことが多い
  - **Responseオブジェクトがthrowされる場合の検証**: loader/actionでは`Response`オブジェクトをthrowするため、`await expect(...).rejects.toThrow(Response)`を使用し、さらに`catch`ブロックで`error.status`や`error.text()`を検証する必要がある。通常のテストでは`Error`をthrowして検証することが多い
  - **リダイレクトの検証方法**: `redirect()`は`Response`オブジェクトを返すため、`response.status`が300-399の範囲であることと`response.headers.get("Location")`を確認する必要がある。通常のテストでは戻り値がオブジェクトやプリミティブであることが多く、このような検証は不要
  - **FormDataの扱い**: actionでは`FormData`を作成して`Request`の`body`に設定する必要がある。通常のテストでは関数の引数としてオブジェクトを直接渡すことが多い
  - **統合テストとしてのデータストアの扱い**: データレイヤー（`posts.server.ts`）をモック化せず、実際のインメモリデータを使用して統合テストとして実装した。通常の単体テストでは依存関係をモック化することが多い
  - loader/actionのテストは、Web標準のAPI（Fetch API）に基づいているため、通常の関数テストとは異なるアプローチが必要になることが理解できた
